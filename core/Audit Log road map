خلي الهدف في الـ AuditLog قابل للضغط (hyperlink)

يعني:

لو الهدف Order → رابط إلى order_detail

لو الهدف Invoice → رابط إلى invoice_detail

لو الهدف JournalEntry → رابط إلى صفحة القيد

لو الهدف Payment → رابط صفحة الدفع

ولو الهدف مش معروف → يظهر نص فقط بدون رابط

هذا يخلي سجل التدقيق عملي جدًا.

إذا تريد هذا: قول

نريد نخلي الهدف في سجل التدقيق رابط

2️⃣ توسعة AuditLog لإضافة extra JSON data

مثل:

old_value / new_value

الفرق بين الحالات (diff)

رقم القيد المحاسبي

رقم الفاتورة

أي قيم إضافية نحتاجها

نستخدم JSONField، وتصير عندك شاشة فيها تفاصيل أعمق (مثل Git diff).

إذا تريده: قول

نريد إضافة تفاصيل أكثر للـ AuditLog

3️⃣ عرض الـ AuditLog في صفحة العنصر نفسه

مثال:

داخل صفحة الفاتورة → تبويب "سجل النشاط"

داخل صفحة الطلب → تبويب "سجل النشاط"

داخل صفحة القيد المحاسبي → تبويب "سجل النشاط"

نجيب فقط السجلات المرتبطة بهذا item.

إذا تباه: قول

نريد سجل التدقيق داخل الصفحات (activity tab)

4️⃣ فلترة وتصدير AuditLog (CSV / Excel)

إضافة زرين:

Export CSV

Export Excel

وتصدير كامل السجل حسب الفلتر.

إذا هذا المطلوب: قول

نريد زر تصدير لسجل التدقيق

توصية للمستقبل

بعد ما سكّرنا الدائرة بين:

🔄 بوابة زبون
🔄 المحاسبة
🔄 الـ Core
🔄 Notifications
🔄 Audit Logs

نقدر نضيف:

Webhook داخلي

Event bus صغير

Plugin apps مستقبلًا مثل WhatsApp Manager

Monitoring أسهل لأن كل شيء مسجل عندك